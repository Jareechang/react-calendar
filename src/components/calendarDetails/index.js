import React from 'react';

const CalendarDetails = (props) => {
    console.log(props.params.dateString)
    return (
        <div>
            details goes here
        </div>
    );
};

export default CalendarDetails;
